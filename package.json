{
  "name": "@inkathon/monorepo",
  "version": "0.0.1",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --filter=frontend",
    "build": "turbo run build --filter=frontend",
    "start": "turbo run start --filter=frontend",
    "lint": "turbo run lint --filter=frontend",
    "lint:fix": "turbo run lint:fix --filter=frontend",
    "prepare": "husky install"
  },
  "pnpm": {
    "peerDependencyRules": {
      "allowedVersions": {
        "react": "^18.0.0",
        "react-dom": "^18.0.0",
        "next": "^13.0.0",
        "@nomiclabs/hardhat-ethers": "0.3.0-beta.13",
        "ethers": "^5.0.0"
      }
    }
  },
  "devDependencies": {
    "husky": "^8.0.2",
    "lint-staged": "^13.0.3",
    "turbo": "^1.6.3",
    "typescript": "^4.8.4",
    "next": "^13.0.3"
  }
}

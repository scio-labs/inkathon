{
  "name": "@inkathon/contracts",
  "license": "MIT",
  "version": "0.0.1",
  "scripts": {
    "build": "cargo +nightly contract build --manifest-path ${CONTRACT:-greeter}/Cargo.toml && pnpm copy-abi",
    "copy-abi": "mkdir -p ./${CONTRACT:-greeter}/abi/ && cp ./${CONTRACT:-greeter}/target/ink/metadata.json ./${CONTRACT:-greeter}/abi/",
    "test": "cargo +nightly contract test --manifest-path ${CONTRACT:-greeter}/Cargo.toml",
    "node": "substrate-contracts-node --dev --port 9944 --base-path ./.node-data",
    "write-address": "mkdir -p ${CONTRACT:-greeter}/deployments/ && echo \"{\n  \\x22address\\x22: \\x22$ADDRESS\\x22\n}\" >${CONTRACT:-greeter}/deployments/${CHAIN:-development}.json",
    "contracts-ui": "open https://weightv1--contracts-ui.netlify.app/?rpc=ws://127.0.0.1:9944",
    "explorer-ui": "open https://polkadot.js.org/apps/#/explorer?rpc=ws://127.0.0.1:9944",
    "dev": "pnpm contracts-ui && pnpm node"
  }
}
